# 🌐 Manalgak Web Standards & Accessibility Guide

이 문서는 프로젝트의 성능, 접근성, 유지보수성을 극대화하기 위한 가이드라인입니다. 모든 개발 작업 시 이 규칙을 준수합니다.

---

## 1. 🏗️ 마크업 (Semantic HTML)
웹 표준을 준수하며, 검색 엔진 최적화(SEO)와 보조 공학 기기(Screen Reader)를 위해 의미 있는 태그를 사용합니다.

- **기본 구조**: `<main>`, `<header>`, `<footer>`, `<nav>`, `<section>` 등을 적절히 사용합니다.
- **제목 체계**: 페이지당 하나의 `<h1>`을 사용하며, `<h2>`~`<h6>` 순차적 계층을 유지합니다.
- **이미지**: `next/image`를 사용하며, 모든 이미지에 구체적인 `alt` 텍스트를 제공합니다. 장식용 이미지는 `alt=""`를 적용합니다.
- **대체 수단**: 텍스트 없이 아이콘만 있는 버튼은 `aria-label` 또는 `title`을 반드시 제공합니다.

## 2. 🏷️ 명명 규칙 (Naming Conventions)
일관된 코드 스타일을 위해 다음 규칙을 준수합니다.

- **CSS 클래스**: 사용자 정의 CSS 클래스는 **`camelCase`**를 사용합니다. (예: `.headerContainer`, `.mainWrapper`)
- **컴포넌트**: React 컴포넌트 파일 및 이름은 **`PascalCase`**를 사용합니다.
- **변수/함수**: 자바스크립트 변수 및 함수는 **`camelCase`**를 사용합니다.

## 3. ♿ 접근성 (Accessibility, A11y)
모든 사용자가 차별 없이 서비스를 이용할 수 있도록 합니다.

- **키보드 접근성**: 모든 인터랙티브 요소(버튼, 링크, 입력창)는 `Tab` 키로 포커스 이동이 가능해야 하며, 시각적인 `:focus-visible` 스타일을 제공합니다.
- **상태 표시**: 모달이나 토글 메뉴 등 상태가 변하는 요소는 `aria-expanded`, `aria-hidden`, `aria-modal` 등 적절한 ARIA 속성을 부여합니다.
- **명도 대비**: 텍스트와 배경 사이의 명도 대비는 WCAG AA 등급(4.5:1 이상)을 권장합니다.
- **폼(Form)**: 모든 `input`은 연결된 `<label>`을 가져야 합니다.

## 3. 🎨 CSS & 디자인 시스템 (Tailwind CSS 4)
일관된 UI 시스템과 성능을 유지합니다.

- **Tailwind 모범 사례**: 중복되는 스타일은 아토믹 클래스 조합보다는 컴포넌트화를 권장합니다. (`clsx`, `tailwind-merge` 활용)
- **반응형 디자인**: Mobile-First 전략을 사용합니다. (`sm:`, `md:`, `lg:` 접두사 활용)
- **크로스 브라우징**: 최신 브라우저 기준의 현대적인 기능을 우선하되, 핵심 기능은 구형 브라우저(Safari 등)에서도 동작하도록 CSS 단위를 신중히 선택합니다. (예: `dvh`, `svh` 등)

## 4. 🚀 성능 및 최적화
- **이미지 최적화**: WebP/Avif 형식을 사용하고 적절한 `sizes` 속성을 정의합니다.
- **애니메이션**: `framer-motion` 사용 시 `layout` 전이와 하드웨어 가속을 활용하며, 불필요한 리렌더링을 방지합니다.

---

> [!TIP]
> 작업을 지시할 때 이 가이드라인을 기반으로 마크업과 스타일을 검토해달라고 요청하세요!
